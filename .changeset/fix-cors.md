---
"api": patch
---

Allow requests from trusted origins, configured via `TRUSTED_ORIGINS`, so the published client can actually be used from a different domain (e.g. a blog or app calling the API from the browser).
